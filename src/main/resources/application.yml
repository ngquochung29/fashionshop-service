spring:
  application:
    name: fashion
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
  datasource:
    url: ${DB_URL}
    username: ${USER}
    password: ${PASS}
    driver-class-name: com.mysql.jdbc.Driver

service:
  secret-key: ${SECRET_KEY}