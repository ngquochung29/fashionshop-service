spring:
  application:
    name: fashion
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.default_schema: dbo
    database: sql_server
    show-sql: true
  datasource:
    url: jdbc:sqlserver://27.72.105.143:1433;databaseName=ShopDior;encrypt=true;trustServerCertificate=true;
    username: sa
    password: Shop123456
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

service:
  secret-key: ${SECRET_KEY}

gcp:
  credentials:
    path: ${StoreKey}
  bucket:
    name: storefile1106

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: debug