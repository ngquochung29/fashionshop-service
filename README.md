Quy chuẩn:
Viết API trong controller k được gọi trực tiếp impl
khai báo static variable trong constant, static function trong util
request, response viết chung trong dto ( k đc trả về entity trực tiếp trong controller)
